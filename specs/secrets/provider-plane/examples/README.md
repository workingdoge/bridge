# Provider Plane Examples

Canonical examples live under [`../../secret-0003/examples/`](../../secret-0003/examples/).

Key examples:

- provider catalog and deployment profile examples;
- attestation results for accepted and denied paths;
- revocation snapshots for clean and revoked paths;
- mode state examples for normal and burn;
- generated provider decisions and audit envelopes;
- audit checkpoint example.

Use these as fixture-level examples of the `SECRET-0003` provider plane, not as
live deployment truth.
